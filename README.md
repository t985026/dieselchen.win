# Dieselchen.win (可以回家啦)

這是一個基於 React 和 Vite 構建的 Web 應用程式，旨在為上班族提供下班倒數計時、午休提醒以及下班後的慶祝動畫與音效。

## 🌟 功能特色

1. **多班次倒數計時**：
    * 支援「正常班」、「晚班」與「金控」三種班制的下班倒數。
2. **自動狀態切換**：
    * **上班中**：顯示疲憊上班族 GIF 與勵志（厭世）語錄。
    * **下班後**：顯示開心回家 GIF 與下班慶祝語錄。
3. **貼心提醒彈窗**：
    * **午休提醒**：每日 12:00 - 13:30 自動跳出休息提示。
    * **下班提醒**：平日 17:30 - 隔日 07:00 自動跳出下班慶祝視窗。
4. **音效與動畫**：
    * 下班時自動播放「叮」提示音效 (使用 Web Audio API 合成)。
    * 下班時刻觸發五彩碎紙與氣球飄浮動畫。
5. **人性化操作**：
    * 彈窗可手動關閉，且在同一時段內不會再次干擾。
6. **每日躺平激勵**：
    * 右下角隨機顯示結合時事與「躺平文化」的幽默勵志語錄，點擊即可切換，為上班生活增添樂趣。

## 🛠️ 技術架構

本專案使用現代前端工具鏈構建：

* **框架**: [React](https://react.dev/)
* **建置工具**: [Vite](https://vitejs.dev/)
* **語言**: JavaScript (ES Modules)

### 目錄結構

```text
dieselchen.win/
├── public/              # 靜態資源 (GIF 圖片)
│   ├── 大家可以回家拉.gif
│   └── 疲憊上班族.gif
├── src/
│   ├── components/      # React 元件
│   │   ├── BlogLink.jsx
│   │   ├── DailyMotivation.jsx
│   │   ├── MainImage.jsx
│   │   ├── Popup.jsx
│   │   ├── Quote.jsx
│   │   └── TimerBox.jsx
│   ├── data/           # 資料檔案
│   │   └── quotes.js   # 語錄資料 (工作、下班、激勵語錄)
│   ├── App.jsx         # 主要應用程式邏輯 (時間判斷、狀態管理)
│   ├── App.css         # 全域樣式設定
│   ├── index.css       # 基礎樣式
│   ├── audioUtils.js   # 音效工具函式
│   └── main.jsx        # 應用程式進入點
├── index.html          # 網頁進入點
├── vite.config.js      # Vite 設定檔
└── package.json        # 專案依賴與腳本
```

## 🚀 部署方式 (Deployment)

本專案採用 **Cloudflare Pages** 進行自動化 CI/CD 部署。

### 部署流程

1. 程式碼推送到 GitHub Repository (Mirror Repo)。
2. Cloudflare Pages 偵測到 Commit 更新。
3. 自動觸發 Build 流程並發佈到 `dieselchen.win`。

### Cloudflare Pages 設定 (必要)

為了確保專案能正確編譯與執行，請確認 Cloudflare Pages 的 **Build settings** 設定如下：

* **Framework preset**: `Vite` (或 None)
* **Build command**: `npm run build`
* **Build output directory**: `dist`
* **Root directory**: `/` (預設為空)

> ⚠️ **注意**：若未設定 Build command，網站將會顯示空白頁面並報錯 `MIME type text/jsx`，因為瀏覽器無法直接執行未編譯的 React 原始碼。

## 💻 本地開發 (Local Development)

若要在本機執行專案：

1. **安裝依賴**：

    ```bash
    npm install
    ```

2. **啟動開發伺服器**：

    ```bash
    npm run dev
    ```

    預設網址為 `http://localhost:5173/`。

3. **建置正式版本**：

    ```bash
    npm run build
    ```

    編譯後的檔案將位於 `dist/` 目錄。
